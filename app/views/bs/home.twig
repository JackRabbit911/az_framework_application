{% extends 'layout.twig' %}

{% block content %}
    <h1 class="display-1 me-1">{{ h1 }}</h1>
    {# {% for lang,language in i18n.linksList %}
        {{ lang }} {{ language }} <br>
    {% endfor %} #}

    {# {{ path('home') }} #}
    {# {{ user.name }} #}
    {# {{ bar }} #}
{% endblock %}
